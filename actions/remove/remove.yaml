runtime: plugin
action:
  title: Remove
  description: Remove package dependency from compose.yaml
  arguments:
    - name: packages
      title: Packages
      description: One or more packages to remove
      type: array
      required: true
  result:
    type: object
    properties:
      packages:
        type: array
        items:
          type: string
